The most recent model is a scikit-learn OLS linear regression model with `x2` and `x4` features using an 80/20 train-test split. It achieved a test mean squared error (MSE) of 55.64. This improves on the previous one-feature model, which used only `x2` and achieved a test MSE of 84.41, reducing the MSE by approximately 34.1% and indicating that adding `x4` improved predictive performance.